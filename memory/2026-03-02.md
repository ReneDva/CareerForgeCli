
## March 2nd, 2026 - Key Session Memories

**Job Search Automation Setup:**
- User requested a recurring job search every 4 hours for 'Junior Developer', 'AI Developer', 'Machine Learning Engineer', 'מפתח ג'וניור', 'מפתח AI', 'מהנדס Machine Learning' roles in 'Tel Aviv', 'Ramat Gan', 'Giv'atayim', 'Petah Tikva', 'Givat Shmuel'.
- Profile path: `C:\Users\rened\Documents\CareerForgeCli\profile.md`.

**Script & Workflow Development:**
- Developed `job_search_wrapper.ps1` to scrape and apply initial filters, saving results to `jobs_found.json`.
- Developed `process_jobs.ps1` to handle job tracking, persistent memory, live link checking, and Telegram notifications.
- Updated `HEARTBEAT.md` to orchestrate the execution of these two scripts.

**Filtering Logic Evolution:**
- **Initial Strict Mode Filters:**
    - Discarded jobs if title contained 'Senior', 'Lead', 'Staff', 'Principal', 'Manager', 'Expert'.
    - Discarded if `date_posted` was null or >24 hours old.
    - Discarded if description explicitly asked for '3+ years experience' or if description was null.
- **Revised Flexible Filters (Current Logic):**
    - Title filter remains strict.
    - **Recency Filter:** Discards only if `date_posted` is explicitly older than 24 hours (not if null or recent). This aims to retain LinkedIn jobs that often lack a `date_posted` from `jobspy`.
    - **Experience Filter:** Discards only if description is present *and* explicitly asks for '3 or more years of experience'. Jobs with null descriptions are *not* discarded at this stage.

**Persistent Memory & CV Tracking:**
- Implemented `job_tracker.csv` (headers: `job_id`, `title`, `company`, `status`, `date_found`, `cv_file_path`) to prevent duplicate notifications and track application status.
- Statuses: 'Sent' (initial notification), 'Applied' (user reaction/approval).
- Created `Generated_CVs` folder for storing tailored CV PDFs.

**Live Link Check:**
- Integrated a live link check in `process_jobs.ps1` using `Invoke-WebRequest`. Jobs with inactive application links are discarded before notification.

**User Feedback (2026-03-02):**
- Job searches should exclude Haifa and focus on the Tel Aviv area.
- Strictly filter out jobs requiring 5+ years of experience as they do not fit the user's profile.
- User requested to apply for the "Junior Software Engineer -Temporary position" at Corephotonics Ltd.
